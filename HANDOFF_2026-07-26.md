# 인수인계 — 2026-07-26 (새벽~정오)

## 1. 결제 (사주홈 sajufortune.kr)

새벽에 990원 이용권 결제가 카드·카카오·무통장 **전부 실패**하던 것을 수리했다.

| 증상 | 원인 | 상태 |
| --- | --- | --- |
| 결제창 자체가 안 열림 | paymentId 51자 > 이니시스 oid 40자 제한 | ✅ 38자 규약으로 교체 |
| "구매자 이름은 필수" | 카드 경로에 `fullName` 누락 | ✅ 결제창에서 직접 입력받게 변경 |
| 구매자명 `rlat * s` | 사주 프로필(localStorage) 이름을 몰래 사용 | ✅ |
| 가상계좌 계좌번호 빈칸 | PortOne V2 SDK 응답엔 계좌 정보 없음 | ✅ 서버 조회(`/pass/va_info`)로 해결 |
| 입금했는데 미지급 | 이니시스 **가상계좌 입금통보 URL 미등록** 정황 | ⏸ 가상계좌 자체를 폐기(`VA_ENABLED=false`) |
| 990원 카드 거절 | KG이니시스 신용카드 최소 1,000원 | ⏸ 버튼 숨김(`CARD_MIN_KRW`), 하향 신청 중 |
| 모바일 키보드가 입력칸 가림 | 키보드가 vh·fixed를 줄이지 않음 | ✅ visualViewport로 해결 |

### 남은 일
1. **포트원 CS 회신 대기** — 카드 최소금액 100원 하향. 승인되면 `static/js/pass_modal.js`의 `CARD_MIN_KRW`를 100으로 낮추고 캐시버스터 bump → 990원 카드 복구.
2. **7/26 새벽 990원 가상계좌 입금건** — 통장에서 나갔는데 포트원에 결제 건 자체가 없다. 반영 또는 환불 요청 필요. 문의 초안: `C:\saju_v6\docs\포트원_문의_가상계좌_미기록_2026-07-26.md`
3. **이니시스 가상계좌 입금통보 URL 등록** — `https://tx-gateway-service.prod.iamport.co/inicis-v2`. 등록하면 `VA_ENABLED=true`로 되살릴 수 있다.
4. **입금문자 자동확인 실입금 검증** — 폰 문자 → Gmail → 서버 3분 폴링까지 구축했으나 **끝단(주문 매칭 → PDF 발송)은 실제 입금으로만 검증된다.** 소액 1건 입금해보면 확인된다.

## 2. 띠별운세 발행 라인

- 생성·발행 기준을 **D+2**로 앞당겼다(하루 실패해도 만회할 여유).
- 영상 길이 상한 **85초** 통일. `zodiac_reels`에 없던 길이 게이트를 신설(95초짜리가 나오던 원인).
- TTS 속도: 95초판 1.12 / 20초판 1.25. 20초판을 **매일** 만들고 유튜브는 **격일 교대**(정책 필터 회피).
- 유령 게시물(Threads ghost post) **매일 아침 8시** 자동 발행 신설.

## 3. 홍보 라인 (신설)

- 3세트(`free`/`pass`/`channel`) 카드 6장 + 쇼츠 23~25초 **모두 생성 완료**.
- 스레드: 매주 토 12:00 자동(`promo-weekly.yml`), 오늘 `free` 세트 12:00 1회 발행 예약.
- 네이버 블로그: **발행 완료** https://blog.naver.com/gsd1004/224358186482
- 다음 블로그 발행: 매월 첫 일요일 07:00. **다음 달 주제 후보**는 원고 하단 메모 참조.

### 확인할 것
- 오늘 12:00 스레드 발행 결과(캐러셀·쇼츠가 계정에 실제로 올라갔는지)
- 오늘 18:00 띠별운세 파이프라인에서 7/28 영상이 **85초 이내**로 나오는지(zodiac_shorts 경로 첫 실측)
- 오늘 21:00 스레드 7/28자 발행

## 4. 관리자 페이지

- 입금대기·풀이대기 목록을 **최신순**으로, 최근 10건만 펼치고 나머지는 **10건 단위로 접기**.
- UTM 소스 표기 통일(`ig`→`instagram` 등), 기존 데이터도 병합.

## 5. 인스타그램 (보류)

계정 3개 중 `sajufortune.kr`(미사용)로 카드·쇼츠를 옮기려던 건은 **중단 상태 그대로**다.
`zodiac_instagram.py`의 `IG_PUBLISH_DISABLED=True`와 워크플로 주석 두 곳을 풀어야 하고,
그 계정의 IG 비즈니스 계정 ID·장기 토큰을 GitHub Secrets에 넣어야 한다(자격증명이라 한밝님이 직접).
