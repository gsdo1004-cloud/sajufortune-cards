
_preview/
__pycache__/

# 2026-07-17 — 이 repo는 PUBLIC. 아래는 절대 올라가면 안 됨.
# ⚠️ .gitignore는 줄 끝 주석을 지원하지 않는다. 주석은 반드시 별도 줄에.

# 로그에 키 앞자리·내부 경로가 찍힘
zodiac_daily.log
# TTS 시험 산출물
_tts_test.*
# 에셋라이브러리 BGM 로컬 캐시 — 음원 공개 금지·용량 폭탄
bgm_cache/
# 조립 중간물(컷 mp4·나레이션 mp3)
cards/*/_shorts/
cards/*/_reel/
.env
credentials.json
promo/*/_tmp/
# 20초판(유튜브 전용) — raw URL 로 참조되지 않는다. 업로더가 로컬 파일을 직접 올린다.
# 95초판(_tts.mp4)과 cards/*.png 는 릴스·카드뉴스 발행이 raw.githubusercontent.com 으로
# 읽어가므로 **절대 빼면 안 된다**.
reels/*_10s.mp4

# 로그·임시
zodiac_task.log
